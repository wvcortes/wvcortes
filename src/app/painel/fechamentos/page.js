import {db} from "@/lib/db";import {semanaAtual} from "@/lib/fechamento";import PainelFechamentos from "./PainelFechamentos";export const dynamic="force-dynamic";export default async function Fechamentos(){const {data=[],error}=await db.from("usuarios").select("id,nome").eq("papel","colaborador").eq("ativo",true).order("nome");if(error)throw new Error("Não foi possível carregar a equipe.");return <><h1 className="text-4xl font-bold">Fechamento semanal</h1><p className="mt-2 text-fumaca">Produção, comissões, pendências e ajustes por colaborador.</p><PainelFechamentos profissionais={data} semana={semanaAtual()}/></>}
